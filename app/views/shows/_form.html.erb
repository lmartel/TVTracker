<%= form_for @show, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :api_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :api_id, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :ended, :class => 'control-label' %>
    <div class="controls">
      <%= f.check_box :ended, :class => 'check_box' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :airtime, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :airtime, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                shows_path, :class => 'btn' %>
  </div>
<% end %>
