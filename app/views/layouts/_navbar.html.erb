<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <%= link_to "TV Tracker", root_path, :class => "brand" %>
      <div class="nav-collapse">
        <ul class="nav">
          <% if current_user %>
            <%= smart_navbar_item "Track Episodes", track_path %>
          <% end %>
          <%= smart_navbar_item "Browse Shows", shows_path %>
          <% if current_user %>
            <li><%= link_to "Logout", logout_path, :method => :delete %></li>
          <% else %>
            <%= smart_navbar_item "Login", login_path %>
            <%= smart_navbar_item "Signup", signup_path %>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</div>